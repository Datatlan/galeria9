---
import Base from '../layouts/Base.astro';

const espacios = [
  { n: 'Terraza', d: 'Un patio verde en planta baja. Íntimo, cálido, ideal para reuniones y talleres.', cap: 'Hasta 40' },
  { n: 'Roof', d: 'Azotea con zona techada y cielo abierto. Para activaciones y celebraciones.', cap: 'Hasta 60' },
  { n: 'Punto Presencia', d: 'El corazón del showcase de marcas. La primera vista al entrar.', cap: 'Showcase' },
  { n: 'Tu Talento, Tu Arte', d: 'Pedestales para exhibir a artistas y creadores locales.', cap: 'Exhibición' },
];

const pilares = [
  { t: 'Bienestar', d: 'Cabinas de psicología, masaje, sound healing y más. Citas con especialistas residentes.' },
  { t: 'Market', d: 'Marcas y talento local en exhibición. Un espacio curado que descubres al visitarnos.' },
  { t: 'Eventos', d: 'Terraza y roof para tus celebraciones, talleres y activaciones de marca.' },
];

const cabinas = [
  { n: 'Fer', marca: 'ferdelamonja', svc: 'Soundhealing · respiración · mindfulness', featured: true },
  { n: 'Terapia', marca: 'Terapia', svc: 'Inconsciente y reprogramación', wa: '33 1000 0001', ig: '@terapia.g9' },
  { n: 'Arigato 88', marca: 'Arigato 88', svc: 'Masajes terapéuticos', wa: '33 1000 0002', ig: '@arigato88' },
  { n: 'Jar Almodovar', marca: 'Jar Almodovar', svc: 'Regeneración nutricional', wa: '33 1000 0003', ig: '@jaralmodovar' },
  { n: 'Ceja & depilación', marca: 'Cabina', svc: 'Diseño en depilación y ceja', wa: '33 1000 0004', ig: '@ceja.g9' },
];
---
<Base>
  <!-- HERO -->
  <section class="hero">
    <div class="wrap hero__inner">
      <span class="eyebrow reveal">Guadalajara · Market de Talentos</span>
      <h1 class="display reveal">Una casa para<br/>el bienestar,<br/>el arte y la<br/>comunidad.</h1>
      <p class="lead hero__lead reveal">Galería 9 reúne cabinas de bienestar, un market de marcas locales y espacios para tus eventos — todo bajo un mismo techo con alma.</p>
      <div class="hero__cta reveal">
        <a href="/cotiza" class="btn">Cotiza tu evento</a>
        <a href="/#experiencia" class="link-arrow">Conoce la casa →</a>
      </div>
    </div>
    <div class="hero__mark" aria-hidden="true">
      <img src="/logo.png" alt="" />
    </div>
  </section>

  <hr class="hairline" />

  <!-- EXPERIENCIA -->
  <section class="section" id="experiencia">
    <div class="wrap">
      <div class="two">
        <div class="reveal">
          <span class="eyebrow">La experiencia</span>
          <h2 class="h2" style="margin-top:1.2rem">Tres mundos, un mismo lugar.</h2>
        </div>
        <p class="lead reveal">No somos una tienda ni un salón de eventos. Somos una casa curada donde el bienestar, las marcas locales y las celebraciones conviven — y se potencian entre sí.</p>
      </div>

      <div class="pilares">
        {pilares.map((p, i) => (
          <article class="pilar reveal" style={`transition-delay:${i * 90}ms`}>
            <span class="pilar__num">{String(i + 1).padStart(2, '0')}</span>
            <h3 class="h3">{p.t}</h3>
            <p class="muted">{p.d}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- GALERÍA / EL LUGAR -->
  <section class="section galeria" id="lugar">
    <div class="wrap">
      <div class="two">
        <div class="reveal">
          <span class="eyebrow">El lugar</span>
          <h2 class="h2" style="margin-top:1.2rem">Un espacio<br/>con alma.</h2>
        </div>
        <p class="lead reveal">Madera, luz cálida y verde. Terraza en planta baja, roof abierto al cielo y rincones curados para exhibir. Así se siente Galería 9 por dentro.</p>
      </div>
    </div>
    <div class="galeria__grid reveal">
      <figure><img src="/fotos/roof-1.jpg" alt="Roof de Galería 9" loading="lazy" /></figure>
      <figure><img src="/fotos/terraza-1.jpg" alt="Terraza de Galería 9" loading="lazy" /></figure>
      <figure><img src="/fotos/popup.jpg" alt="Zona Pop Up" loading="lazy" /></figure>
      <figure><img src="/fotos/terraza-2.jpg" alt="Terraza · detalle" loading="lazy" /></figure>
      <figure><img src="/fotos/invitado.jpg" alt="Invitado Especial" loading="lazy" /></figure>
      <figure><img src="/fotos/roof-2.jpg" alt="Roof · vista" loading="lazy" /></figure>
      <figure><img src="/fotos/terraza-3.jpg" alt="Terraza · rincón" loading="lazy" /></figure>
    </div>
  </section>

  <!-- CABINAS -->
  <section class="section cabinas" id="cabinas">
    <div class="wrap">
      <div class="two">
        <div class="reveal">
          <span class="eyebrow">Bienestar · Planta alta</span>
          <h2 class="h2" style="margin-top:1.2rem">Nuestras<br/>cabinas.</h2>
        </div>
        <p class="lead reveal">Marcas residentes de bienestar, cada una con su especialidad. Reserva con Fer muy pronto en línea; las demás, en camino.</p>
      </div>

      <div class="cabgrid">
        {cabinas.map((c, i) => (
          <article class={`cab reveal ${c.featured ? 'cab--fer' : ''}`} style={`transition-delay:${i * 70}ms`}>
            <span class="cab__tag">Cabina · {c.marca}</span>
            <h3 class="cab__name">{c.n}</h3>
            <p class="cab__svc">{c.svc}</p>
            {c.featured
              ? <span class="cab__cta">Reserva en línea · muy pronto</span>
              : <div class="cab__contact"><span>WhatsApp · {c.wa}</span><span class="cab__ig">{c.ig}</span></div>}
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- ESPACIOS -->
  <section class="section espacios" id="espacios">
    <div class="wrap">
      <div class="two">
        <div class="reveal">
          <span class="eyebrow">Los espacios</span>
          <h2 class="h2" style="margin-top:1.2rem">Cada rincón,<br/>una posibilidad.</h2>
        </div>
        <p class="lead reveal">Desde un patio íntimo hasta una azotea abierta al cielo. Elige el escenario para tu próxima experiencia.</p>
      </div>

      <div class="grid-esp">
        {espacios.map((e, i) => (
          <article class="esp reveal" style={`transition-delay:${i * 80}ms`}>
            <div class="esp__top">
              <h3 class="h3">{e.n}</h3>
              <span class="esp__cap">{e.cap}</span>
            </div>
            <p class="muted">{e.d}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="cta-band">
    <div class="wrap cta-band__inner reveal">
      <h2 class="h2">¿Tienes un evento en mente?</h2>
      <p class="lead">Cuéntanos qué imaginas y arma tu cotización en un par de minutos.</p>
      <a href="/cotiza" class="btn btn--invert">Cotiza tu evento</a>
    </div>
  </section>
</Base>

<style>
  .hero { position: relative; overflow: clip; padding-block: clamp(3.5rem, 9vw, 7rem) clamp(4rem, 10vw, 8rem); }
  .hero__inner { position: relative; z-index: 2; }
  .hero .display { margin-top: 1.6rem; }
  .hero__lead { max-width: 34ch; margin-top: 2rem; }
  .hero__cta { display: flex; align-items: center; gap: 2rem; margin-top: 2.8rem; flex-wrap: wrap; }
  .hero__mark {
    position: absolute; z-index: 1; top: 50%; right: -6%; transform: translateY(-50%);
    width: min(46vw, 560px); opacity: .07; pointer-events: none;
  }

  .two { display: grid; grid-template-columns: 1fr 1fr; gap: clamp(2rem, 5vw, 5rem); align-items: end; margin-bottom: clamp(3rem, 6vw, 5rem); }

  .pilares { display: grid; grid-template-columns: repeat(3, 1fr); gap: clamp(1.5rem, 4vw, 3.5rem); }
  .pilar { border-top: 1px solid var(--ink); padding-top: 1.4rem; }
  .pilar__num { font-size: .8rem; letter-spacing: .2em; color: var(--gold-ink); }
  .pilar h3 { margin: 1.2rem 0 .8rem; }

  .cabgrid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 1rem; }
  .cab {
    display: flex; flex-direction: column; gap: .5rem;
    padding: 1.6rem 1.4rem; min-height: 210px;
    border: 1px solid var(--ink-12); border-radius: var(--radius); background: var(--white);
    transition: border-color .3s var(--ease), transform .3s var(--ease);
  }
  .cab:hover { border-color: var(--ink-45); transform: translateY(-3px); }
  .cab__tag { font-size: .68rem; letter-spacing: .14em; text-transform: uppercase; color: var(--gold-ink); }
  .cab__name { font-size: 1.4rem; font-weight: 300; margin: .2rem 0; }
  .cab__svc { font-size: .88rem; color: var(--ink-70); line-height: 1.4; }
  .cab__contact { margin-top: auto; display: flex; flex-direction: column; gap: .2rem; font-size: .78rem; color: var(--ink-70); letter-spacing: .01em; }
  .cab__ig { color: var(--gold-ink); }
  .cab__cta { margin-top: auto; font-size: .72rem; letter-spacing: .1em; text-transform: uppercase; color: var(--gold); font-weight: 500; }
  .cab--fer { background: var(--ink); color: var(--paper); border-color: var(--ink); }
  .cab--fer .cab__tag { color: var(--gold); }
  .cab--fer .cab__svc { color: rgba(242,242,242,.72); }

  .grid-marcas { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem; }
  .marca { display: flex; flex-direction: column; gap: .6rem; padding: 1.8rem 1.6rem; border: 1px solid var(--ink-12); border-radius: var(--radius); background: var(--white); transition: border-color .3s var(--ease), transform .3s var(--ease); }
  .marca:hover { border-color: var(--ink); transform: translateY(-3px); }
  .marca__top { display: flex; justify-content: space-between; align-items: baseline; gap: 1rem; }
  .marca__meta { font-size: .7rem; letter-spacing: .14em; text-transform: uppercase; color: var(--gold-ink); white-space: nowrap; }
  .marca .muted { flex: 1; font-size: .92rem; }
  .marca__cta { font-size: .78rem; letter-spacing: .12em; text-transform: uppercase; font-weight: 500; color: var(--ink); margin-top: .3rem; }

  .galeria__grid { columns: 3; column-gap: 10px; padding-inline: clamp(1.25rem, 5vw, 4rem); margin-top: clamp(2rem, 5vw, 3rem); }
  .galeria__grid figure { margin: 0 0 10px; break-inside: avoid; overflow: hidden; border-radius: var(--radius); background: var(--sand); }
  .galeria__grid img { width: 100%; display: block; transition: transform .8s var(--ease); }
  .galeria__grid figure:hover img { transform: scale(1.04); }

  .espacios { background: var(--sand); }
  .grid-esp { display: grid; grid-template-columns: 1fr 1fr; gap: 1px; background: var(--ink-12); border: 1px solid var(--ink-12); }
  .esp { background: var(--sand); padding: clamp(1.6rem, 3vw, 2.6rem); }
  .esp__top { display: flex; justify-content: space-between; align-items: baseline; gap: 1rem; margin-bottom: .8rem; }
  .esp__cap { font-size: .72rem; letter-spacing: .16em; text-transform: uppercase; color: var(--gold-ink); white-space: nowrap; }

  .cta-band { background: var(--ink); color: var(--paper); padding-block: clamp(4rem, 9vw, 7rem); text-align: center; }
  .cta-band .h2 { color: var(--paper); }
  .cta-band .lead { color: rgba(242,242,242,.7); margin: 1.2rem auto 2.4rem; max-width: 42ch; }
  .btn--invert { background: var(--paper); color: var(--ink); border-color: var(--paper); }
  .btn--invert:hover { background: transparent; color: var(--paper); }

  @media (max-width: 980px) { .cabgrid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 760px) {
    .two { grid-template-columns: 1fr; align-items: start; }
    .pilares { grid-template-columns: 1fr; }
    .grid-esp { grid-template-columns: 1fr; }
    .grid-marcas { grid-template-columns: 1fr; }
    .galeria__grid { columns: 2; }
    .cabgrid { grid-template-columns: 1fr; }
    .hero__mark { opacity: .05; right: -20%; }
  }
</style>
